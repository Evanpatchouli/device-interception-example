HOST=127.0.0.1
PORT=30016
PROTOCOL=http
# Comma-separated browser origins allowed to call the API and Socket.IO server.
# Leave empty to allow only localhost, 127.0.0.1, and [::1] on PORT.
ALLOWED_ORIGINS=
